import { FollowedArtist, Order } from "./types";

export const mockCollectorName = "Adrienne Kessler";

// Mock collector records for the /dashboard route (kept in code, not linked).
// The previous entries pointed at the removed fictional catalogue.
export const orders: Order[] = [];

export const favoriteArtworkIds: string[] = [];

export const followedArtists: FollowedArtist[] = [];
